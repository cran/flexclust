useDynLib(flexclust)

import(graphics)
import(grid)
import(stats)

exportPattern("^dist.*", "^cent.*")

export(
       as.kcca,
       cclust,
       infoCheck,
       kcca,
       kccaFamily,
       projAxes,
       qtclust,
       stepFlexclust
       )


exportClasses(
              cclustControl,
              flexclust,
              flexclustControl,
              kcca,
              kccaFamily,
              projAxes,
              stepFlexclust
              ) 

exportMethods(
              barplot,
              cluster,
              coerce,
              getModel,
              image,
              info,
              pairs,
              parameters,
              placeLabels,
              plot,
              predict,
              show,
              summary
              )


S3method(as.kcca, kmeans)
S3method(as.kcca, partition)


