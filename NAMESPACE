useDynLib(flexclust)

import(graphics)
import(grid)
import(lattice)
import(methods)
import(modeltools)
import(stats)

exportPattern("^dist.*", "^cent.*")

export(
       as.kcca,
       cclust,
       flxColors,
       kcca,
       kccaFamily,
       projAxes,
       qtclust,
       stepFlexclust
       )


exportClasses(
              cclustControl,
              flexclust,
              flexclustControl,
              kcca,
              kccasimple,
              kccaFamily,
              projAxes,
              stepFlexclust
              ) 

exportMethods(
              barchart,
              barplot,
              cluster,
              coerce,
              getModel,
              image,
              info,
              pairs,
              parameters,
              placeLabels,
              plot,
              predict,
              show,
              summary
              )


S3method(as.kcca, kmeans)
S3method(as.kcca, partition)


