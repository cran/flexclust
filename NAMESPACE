useDynLib(flexclust)

import(methods)
import(graphics)
import(grid)
import(lattice)
import(modeltools)

importFrom(stats, fitted, predict)
importFrom(stats4, summary)

exportPattern("^dist.*", "^cent.*", "^panelShadow.+")

export(
       as.kcca,
       bootFlexclust,
       bundestag,
       cclust,
       clusterSim,
       flxColors,
       kcca,
       kccaFamily,
       projAxes,
       propBarchart,
       qtclust,
       randIndex,
       randomTour,
       randomTourMatrix,
       shadow,
       shadowStars,
       Silhouette,
       stepFlexclust,
       stripes
       )


exportClasses(
              bootFlexclust,
              cclustControl,
              flexclust,
              flexclustControl,
              kcca,
              kccasimple,
              kccaFamily,
              projAxes,
              propBarchart,
              stepFlexclust
              ) 

exportMethods(
              "[[",
              barchart,
              barplot,
              boxplot,
              clusters,
              clusterSim,
              coerce,
              densityplot,
              getModel,
              image,
              info,
              pairs,
              parameters,
              placeLabels,
              plot,
              predict,
              randIndex,
              randomTour,
              relabel,
              shadow,
              Silhouette,
              show,
              summary
              )


S3method(as.kcca, kmeans)
S3method(as.kcca, partition)


